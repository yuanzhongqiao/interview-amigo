name: Perfect Interview Actions
run-name: ${{ github.actor }} is testing out Perfect Interview Actions 🚀
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Build
        run: npm run build
      - name: Run dev
        run: npm run dev
      # - name: Install yarn
      #   run: npm i -g yarn
      # - name: Clone project
      #   run: git clone https://github.com/top329/perfect_interview.git && cd perfect_interview
      # - name: Install dependencies
      #   run: yarn
      # - name: Linting
      #   run: yarn lint
      # - name: Build
      #   run: yarn build
      # - name: Run dev
      #   run: yarn dev
